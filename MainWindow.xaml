<Window x:Class="wpf_projcts.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpf_projcts"
        mc:Ignorable="d"
        Title="Markdown 编辑器"
        Height="720" Width="1150"
        WindowStartupLocation="CenterScreen"
        AllowDrop="True"
        Drop="Window_Drop"
        Closing="Window_Closing">
    <Window.InputBindings>
        <KeyBinding Key="N" Modifiers="Control" Command="{Binding NewDocumentCommand}"/>
        <KeyBinding Key="O" Modifiers="Control" Command="{Binding OpenDocumentCommand}"/>
        <KeyBinding Key="S" Modifiers="Control" Command="{Binding SaveDocumentCommand}"/>
        <KeyBinding Key="W" Modifiers="Control" Command="{Binding CloseTabCommand}"/>
    </Window.InputBindings>

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="文件(_F)">
                <MenuItem Header="新建(_N)" InputGestureText="Ctrl+N" Command="{Binding NewDocumentCommand}"/>
                <MenuItem Header="打开(_O)" InputGestureText="Ctrl+O" Command="{Binding OpenDocumentCommand}"/>
                <Separator/>
                <MenuItem Header="保存(_S)" InputGestureText="Ctrl+S" Command="{Binding SaveDocumentCommand}"/>
                <MenuItem Header="另存为(_A)" Command="{Binding SaveDocumentAsCommand}"/>
                <Separator/>
                <MenuItem Header="退出(_X)" Command="{Binding ExitCommand}"/>
            </MenuItem>
            <MenuItem Header="编辑(_E)">
                <MenuItem Header="撤销(_U)" Command="ApplicationCommands.Undo"/>
                <MenuItem Header="重做(_R)" Command="ApplicationCommands.Redo"/>
                <Separator/>
                <MenuItem Header="剪切(_T)" Command="ApplicationCommands.Cut"/>
                <MenuItem Header="复制(_C)" Command="ApplicationCommands.Copy"/>
                <MenuItem Header="粘贴(_P)" Command="ApplicationCommands.Paste"/>
            </MenuItem>
            <MenuItem Header="格式(_O)">
                <MenuItem Header="标题(_H)">
                    <MenuItem Header="一级标题" Click="Heading_Click" Tag="# "/>
                    <MenuItem Header="二级标题" Click="Heading_Click" Tag="## "/>
                    <MenuItem Header="三级标题" Click="Heading_Click" Tag="### "/>
                    <MenuItem Header="四级标题" Click="Heading_Click" Tag="#### "/>
                    <MenuItem Header="五级标题" Click="Heading_Click" Tag="##### "/>
                    <MenuItem Header="六级标题" Click="Heading_Click" Tag="###### "/>
                </MenuItem>
                <Separator/>
                <MenuItem Header="加粗(_B)" Click="Bold_Click"/>
                <MenuItem Header="斜体(_I)" Click="Italic_Click"/>
                <MenuItem Header="行内代码(_C)" Click="Code_Click"/>
                <MenuItem Header="代码块" Click="CodeBlock_Click"/>
                <Separator/>
                <MenuItem Header="链接(_L)" Click="Link_Click"/>
                <MenuItem Header="图片(_M)" Click="Image_Click"/>
                <Separator/>
                <MenuItem Header="无序列表" Click="Bullet_Click"/>
                <MenuItem Header="有序列表" Click="Numbered_Click"/>
                <MenuItem Header="引用" Click="Quote_Click"/>
                <MenuItem Header="分割线" Click="Hr_Click"/>
            </MenuItem>
            <MenuItem Header="帮助(_H)">
                <MenuItem Header="关于(_A)" Click="About_Click"/>
            </MenuItem>
        </Menu>

        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Content="新建" ToolTip="新建 (Ctrl+N)" Command="{Binding NewDocumentCommand}"/>
                <Button Content="打开" ToolTip="打开 (Ctrl+O)" Command="{Binding OpenDocumentCommand}"/>
                <Button Content="保存" ToolTip="保存 (Ctrl+S)" Command="{Binding SaveDocumentCommand}"/>
                <Separator/>
                <Button Content="H1" ToolTip="一级标题" Click="Heading_Click" Tag="# "/>
                <Button Content="B" ToolTip="加粗" Click="Bold_Click"/>
                <Button Content="I" ToolTip="斜体" Click="Italic_Click"/>
                <Button Content="`代码`" ToolTip="行内代码" Click="Code_Click"/>
                <Button Content="代码块" ToolTip="代码块" Click="CodeBlock_Click"/>
                <Button Content="链接" ToolTip="插入链接" Click="Link_Click"/>
                <Button Content="图片" ToolTip="插入图片" Click="Image_Click"/>
                <Separator/>
                <Button Content="• 列表" ToolTip="无序列表" Click="Bullet_Click"/>
                <Button Content="1. 列表" ToolTip="有序列表" Click="Numbered_Click"/>
                <Button Content="❝" ToolTip="引用" Click="Quote_Click"/>
                <Button Content="──" ToolTip="分割线" Click="Hr_Click"/>
            </ToolBar>
        </ToolBarTray>

        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Text="{Binding SelectedDocument.StatusLine, StringFormat='行 {0}'}"/>
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text="{Binding SelectedDocument.StatusColumn, StringFormat='列 {0}'}"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="{Binding SelectedDocument.WordCount, StringFormat='字数 {0}'}"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock Text="{Binding SelectedDocument.FilePathText}" TextTrimming="CharacterEllipsis" MaxWidth="420"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="{Binding SelectedDocument.SaveStatus}"/>
            </StatusBarItem>
        </StatusBar>

        <TabControl x:Name="TabHost"
                    ItemsSource="{Binding Documents}"
                    SelectedItem="{Binding SelectedDocument}"
                    SelectionChanged="TabHost_SelectionChanged">
            <TabControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding DisplayTitle}"/>
                        <Button Content="✕"
                                FontSize="10"
                                Padding="4,0"
                                Margin="8,0,0,0"
                                ToolTip="关闭 (Ctrl+W)"
                                Command="{Binding DataContext.CloseTabCommand, RelativeSource={RelativeSource AncestorType=TabControl}}"
                                CommandParameter="{Binding}"/>
                    </StackPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>
        </TabControl>
    </DockPanel>
</Window>
